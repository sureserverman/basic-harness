# Home

Map-of-content for this vault. Hand-curated. The `obsidian-wiki` plugin's `rebuild-home` command can refresh tables in-place but never reorders or removes rows you've added.

This file starts mostly empty. After your first ingest, run `/obsidian-wiki:rebuild-home` to populate the category tables.

---

## Categories

(Tables appear here after the first ingest. Each top-level category in `CLAUDE.md` gets its own table.)

---

## Recent activity

See `log.md` for the full append-only history.
